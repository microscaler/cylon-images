{
  "name": "file_read",
  "description": "Reads the contents of a file at the specified absolute path.",
  "executable_path": "/opt/cylon/bin/file_read.py",
  "schema": {
    "type": "object",
    "properties": {
      "path": { "type": "string" }
    },
    "required": ["path"]
  }
}
